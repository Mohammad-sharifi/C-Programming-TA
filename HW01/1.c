#include <stdio.h>

int main()
{
    // more de 1-a
    
    // float x, y;
    
    // y = 3*x/2 - 1;
    

    // ------------------------
    
    // more de 1-b
    
    // int x, y, z, h;
    
    // z = x*x - (y*y)*(h*h*h);
    
    // ------------------------
    
    // more de 1-c
    
    // float x, y, k, f, d;
    
    // d = (x*x - y*y*y)/(k*k) + f*(-1*x - 5);
    
    // ------------------------
    
    return 0;
}
